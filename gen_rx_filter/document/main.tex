\documentclass[11pt, a4paper]{article}

\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage[hidelinks]{hyperref}
\usepackage{geometry}
\geometry{margin=2.5cm}

\title{Equalizer Lowpass FIR Filter Design via Complex Remez Algorithm}
\author{}
\date{}

\begin{document}

\maketitle

\section{Complex Remez Algorithm}

The complex Remez algorithm (also known as Lawson's iteratively reweighted
least-squares, IRLS) extends the classical Parks--McClellan algorithm to
complex-valued FIR filters.  At each iteration the algorithm solves a
weighted least-squares problem
\[
  \min_{\mathbf{h}} \sum_k w_k(\ell)\,
    \bigl\lvert \mathbf{a}(\omega_k)^\top \mathbf{h} - H_d(\omega_k) \bigr\rvert^2,
\]
and then updates the weights as
$w_k(\ell+1) = w_k(\ell)\,\lvert e_k(\ell) \rvert$,
where $e_k(\ell)$ is the error at frequency $\omega_k$ after iteration
$\ell$.  This reweighting progressively concentrates the design effort on
the worst-case frequency points, driving the solution toward the
minimax (Chebyshev) optimum.

No explicit passband or stopband tolerance parameters are required; the
algorithm converges to the equi-ripple solution determined solely by the
frequency grid and the desired response $H_d(\omega)$.

\subsection{Cosine Transition-Band Constraint}

Without a transition-band constraint the IRLS solver leaves the gap between
the passband and stopband entirely unconstrained, which can cause the
magnitude to overshoot or roll off slowly in that region.  A soft constraint
is introduced by appending a transition-band grid $\mathcal{T}$ to the
unified frequency grid and supplying a cosine-tapered desired magnitude
$H_d^{(\mathrm{tb})}(\omega)$ that interpolates smoothly between the
passband-edge level and the stopband target.  Specifically, for a
normalised parameter $t \in [0, 1]$ sweeping from the passband edge to the
stopband edge,
\[
  H_d^{(\mathrm{tb})}(t)
  = A_{\mathrm{stop}}
    + \bigl(A_{\mathrm{pass}} - A_{\mathrm{stop}}\bigr)
      \cdot \frac{1 + \cos(\pi t)}{2},
\]
where $A_{\mathrm{pass}} = \max_{k\in\mathcal{P}}|H_d(\omega_k)|$ is the
passband-edge magnitude and $A_{\mathrm{stop}} = \delta_s$ is the stopband
target.  This raised-cosine shape has zero derivative at both endpoints,
giving a gentle rolloff at the passband edge and a smooth approach to the
stopband level without abrupt kinks.  The transition-band points enter the
IRLS cost with the same band weight as the stopband ($B_k = w_{\mathrm{sb}}$),
so the solver tracks the ramp target directly rather than simply suppressing
energy toward zero.

\newpage
\section{Simulation}

Figure~\ref{fig:rx_srx} shows the measured magnitude and phase response of
the RX and Switched RX (SRX) paths, captured with a network analyser.
Figure~\ref{fig:ch_eqz} overlays the channel response and its inverse
equalizer response, both DC-normalised.  The equalizer compensates for the
channel magnitude roll-off across the passband.
Figure~\ref{fig:eqz_final} shows the desired equalizer response $H_d(\omega)$
after adding a linear-phase trend, which shifts the target to a causal FIR
training target.
Figures~\ref{fig:filter_response} and~\ref{fig:filter_response_inband} show
the trained FIR filter response.  The filter is designed by the complex Remez
algorithm to match the desired equalizer response $H_d(\omega)$.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../figure/complex_remez_result/rx_srx.png}
  \caption{Measured RX and Switched RX (SRX) frequency responses from the
           network analyser.  Top: magnitude (dB).  Bottom: unwrapped phase (deg).}
  \label{fig:rx_srx}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../figure/complex_remez_result/rx_ch1_h3_ch_eqz.png}
  \caption{Channel and equalizer frequency responses overlaid.
           Top: magnitude (dB).  Bottom: phase (deg).}
  \label{fig:ch_eqz}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../figure/complex_remez_result/rx_ch1_h3_eqz_final.png}
  \caption{Desired equalizer frequency response $H_d(\omega)$ with linear-phase
           trend applied.  Top: magnitude (dB).  Bottom: phase (deg).}
  \label{fig:eqz_final}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../figure/complex_remez_result/filter_response.png}
  \caption{Full-band magnitude (top) and phase (bottom) of the trained
           equalizer FIR filter.}
  \label{fig:filter_response}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../figure/complex_remez_result/filter_response_inband.png}
  \caption{In-band magnitude response of the trained equalizer FIR filter,
           zoomed to the passband region.}
  \label{fig:filter_response_inband}
\end{figure}

\end{document}
